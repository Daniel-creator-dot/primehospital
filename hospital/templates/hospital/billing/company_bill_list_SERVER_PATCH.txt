SERVER PATCH for VariableDoesNotExist (payer)
=============================================
The server at 192.168.2.216 is using an OLD copy of company_bill_list.html.
You must update the file ON THE SERVER (in Docker: /app/hospital/templates/hospital/billing/company_bill_list.html).

REPLACE line 65 completely.

FIND (old line 65):
<td>{{ statement.corporate_account.company_name|default:statement.payer.name|default:"N/A" }}</td>

REPLACE WITH:
<td>{% if statement.corporate_account %}{{ statement.corporate_account.company_name|default:"N/A" }}{% else %}N/A{% endif %}</td>

---
How to apply on server:
1. If using Docker: docker exec -it <container_name> sh, then edit the file with vi/nano, or copy the file from your dev machine into the container.
2. Or rebuild/restart the container after pulling the latest code so it uses the template from your repo.
3. Or scp/copy hospital/templates/hospital/billing/company_bill_list.html from this project to the server at /app/hospital/templates/hospital/billing/
