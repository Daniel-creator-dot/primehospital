# HMS Production Requirements - MySQL Version
# Use this for managed servers (cPanel, Plesk, etc.) with MySQL

# Build Tools
setuptools<81  # Pin to avoid pkg_resources deprecation warning from rest_framework_simplejwt

# Core Django
Django==4.2.7
djangorestframework==3.14.0
django-cors-headers==4.3.1
django-extensions==3.2.3
django-filter==23.3

# Database - MySQL
mysqlclient==2.2.0  # MySQL adapter for managed servers
dj-database-url==2.1.0  # Database URL parsing

# Redis & Caching (optional but recommended)
django-redis==5.4.0
redis==5.0.1

# Environment Variables
python-decouple==3.8
python-dotenv==1.0.0

# API & Authentication
djangorestframework-simplejwt==5.3.0
django-guardian==2.4.0

# Django Allauth (Social Authentication)
django-allauth==0.57.0

# Forms & UI
django-crispy-forms==2.1
crispy-bootstrap5==1.0.0
django-bootstrap5==23.3
django-widget-tweaks==1.5.0

# File Handling
Pillow==10.1.0
openpyxl==3.1.2

# PDF Generation
reportlab==4.0.7
xhtml2pdf==0.2.13

# Excel Import/Export
django-import-export==3.3.3
tablib==3.5.0

# SMS & Communication
twilio==8.10.0
requests==2.31.0

# Date & Time
python-dateutil==2.8.2
pytz==2023.3.post1

# Utilities
shortuuid==1.0.11

# Tables & UI
django-tables2==2.7.0

# Security
cryptography==41.0.7

# History & Audit
django-simple-history==3.4.0

# Celery (Background Tasks)
celery==5.3.4
django-celery-beat==2.5.0
django-celery-results==2.5.1

# Production Server
gunicorn==21.2.0  # WSGI HTTP Server
whitenoise==6.6.0  # Static file serving

# Health Checks
django-health-check==3.18.1

# Monitoring (Optional but recommended)
django-prometheus==2.3.1
sentry-sdk==1.38.0  # Error tracking

# Two-Factor Authentication
django-otp==1.3.0

# Model Utilities
django-model-utils==4.3.1
















